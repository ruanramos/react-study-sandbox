import React from "react";

export const Grid = (props) => {

    
    
    const renderRow = () => {
        return (
            <div className="row"></div>
        )
    };

    return (
        <div>
            {}
        </div>
    );
};